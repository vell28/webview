import { defineMessages } from 'react-intl';

export default defineMessages({
    title: {
        id            : 'src.auth.reset.password.title',
        defaultMessage: 'New password'
    },
    text: {
        id            : 'src.auth.reset.password.text',
        defaultMessage: 'Please enter new password'
    }
});
