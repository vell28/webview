import { defineMessages } from 'react-intl';

export default defineMessages({
    countryPlaceholder: {
        id            : 'src.profile.phone.country',
        defaultMessage: 'CHOOSE A COUNTRY'
    },
});
