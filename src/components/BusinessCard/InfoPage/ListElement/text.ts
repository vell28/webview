import messages from '../messages';
import { translateText } from '@/utils/format-text';

const text = {
    first:  translateText(messages.listText_1),
    second: translateText(messages.listText_2),
    third: translateText(messages.listText_3),
    fourth: translateText(messages.listText_4),
    fifth: translateText(messages.listText_5)
}

export default text;
